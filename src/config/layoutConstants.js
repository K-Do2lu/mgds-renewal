/**
 * JS 레이아웃 값. `920px`($bp-tab) 초과가 데스크톱 헤더/메가메뉴 구간.
 * @see src/assets/scss/abstract/_breakpoints.scss — $bp-tab
 */
export const NAV_WIDE_MIN_WIDTH_PX = 921

/** 메가메뉴 닫힘 지연 (호버 아웃) */
export const HEADER_MEGA_CLOSE_DELAY_MS = 160

/** 헤더 높이 측정 전 초기 mega 패널 top 오프셋 */
export const HEADER_MEGA_FALLBACK_TOP_PX = 72
